import 'package:flutter/material.dart';

class HSpacer extends StatelessWidget {
  const HSpacer({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox(
      width: 16.0,
    );
  }
}
