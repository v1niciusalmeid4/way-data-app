import 'package:core/app_way_data_core.dart';
import 'package:flutter/material.dart';

class RouterStableState extends StatelessWidget {
  final RouterBloc bloc;
  final Stable state;

  const RouterStableState({required this.bloc, required this.state, super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
